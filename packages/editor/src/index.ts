// @ham/editor — one collaborative, block-centric markdown surface.
//
// This is the package entry point. Real exports are added per implementation
// phase (block identity, snapshots, annotations, the HamEditor component, and
// collaboration). The scaffolded stub keeps the package installable and the
// build/test toolchain green from day one.

export const HAM_EDITOR_VERSION = "0.1.0";
